erstes Kapitel (aber brauche ich bestimmt nicht)
es gibt 2 Arten von methoden: supervised und unsupervised, je nachdem ob trainingset da ist oder nicht.
	-> ich brauche wohl eher supervised, da ich die verschiedenen zustände der session als vektor darstellen kann und es gibt dann eine feste anzahl an vektoren
	(wenn ich wirklich die potenzmenge der vektoren brauche könnte ich mir ein schnelles python skript schreiben, das das die potenzmenge berechnet und das zb csv abspeichert)

wenn ich z.b. den einträgen einen vektor zuordne, und dieser vektor beschreibt welche widgets aktiviert sind, dann könnte ich auch abstände zwischen den vektoren berechnen. was genau mir das bringt weiß ich nicht aber so als erste idee kann ich das bestimmt mal fest halten. irgendwie muss in dem vektor dann aber auch fest gehalten werden, ob das dashboard in irgend einer art und weise konfiguriert wurde oder nicht. was ich aber auf jeden fall machen kann ist, dass ich mir sowas wie einen threshold wert überlege und dann berechne ich die distanzen zwischen vektor und threshold und wenn eine bestimmt distanz erreicht ist kann dann die meldung an den kunden raus. dieses distanz berechnen scheint generell eine recht bekannte methode in PR zu sein. hier muss ich dann vielleicht auch mal nach diesem k-means schauen

########################
similarity measures and clustering of string patterns
s. 160: es gibt 3 perspektiven um an pattern resemblance ran zu gehen. welche davon trifft am ehesten auf mich zu?

